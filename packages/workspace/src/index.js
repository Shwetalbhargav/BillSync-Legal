import { packageBoundary } from '../../shared/src/index.js';

export const WORKSPACE_PACKAGE = packageBoundary('@lexora/workspace', ['workspace-context', 'workspace-catalog', 'workspace-isolation']);
