import { packageBoundary } from '../../shared/src/index.js';

export const NOTIFICATIONS_PACKAGE = packageBoundary('@lexora/notifications', ['notification-preferences', 'delivery-events', 'digests']);
